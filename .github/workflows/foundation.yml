name: ArachOS base checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  whitespace:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - run: git diff --check
