---
mode: required
method: legacy
labels:
  step: "Bootloader"
default: grub
items:
  - id: grub
    name: "GRUB"
    description: "Default bootloader for BIOS and UEFI systems."
  - id: systemd-boot
    name: "systemd-boot"
    description: "UEFI-only bootloader for systems with an EFI System Partition."
